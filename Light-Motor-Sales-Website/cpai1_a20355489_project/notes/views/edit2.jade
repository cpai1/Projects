extends layout

block content
  .row
    .col-md-6
      h1 Edit a CAR
      form(role="form", method="post", action="/cars/#{car.id}?_method=put")
        include editform2
        button.btn.btn-primary(type="submit") Edit Car
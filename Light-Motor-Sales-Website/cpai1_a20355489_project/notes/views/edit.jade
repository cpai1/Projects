extends layout

block content
  .row
    .col-md-6
      h1 Edit a NOTE
      form(role="form", method="post", action="/notes/#{note.id}?_method=put")
        include editform
        button.btn.btn-primary(type="submit") Edit Note